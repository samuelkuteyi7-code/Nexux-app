Key principle: the AI layer (`backend/app/simulation/narrative.py`)
generates situations, but the **simulation engine**
(`backend/app/simulation/engine.py`) is the only thing that changes numbers.

## Running it

### Backend
```bash
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload
